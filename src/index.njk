---
layout: layouts/homepage.njk
metas:
  title: Home - Leeds Digital Festival data microsite
---


{% comp 'home.home-grid'%}
  {% comp 'home.panel' %}
    <span class="title">Events hosted</span>
    <div class="number">{{ summary.total_events | localize }}</div>
  {% endcomp %}
  {% comp 'home.panel' %} 
    <span class="title">Event hosts</span>
    <div class="number">{{ summary.total_event_hosts | localize }}</div>
  {% endcomp %}
    {% comp 'home.panel' %}
    <span class="title">Current sponsors</span>
    <div class="number">{{ summary.current_sponsors | localize }}</div>
  {% endcomp %}
    {% comp 'home.panel' %}
    <span class="title">Twitter impressions</span>
    <div class="number">{{ summary.total_twitter_impressions | localize }}</div>
  {% endcomp %}
    {% comp 'home.panel' %}
    <span class="title">Twitter engagements</span>
    <div class="number">{{ summary.total_twitter_engagements | localize }}</div>
  {% comp 'home.panel' %}
    <span class="title">Total website views</span>
    <div class="number">{{ summary.total_website_views | localize }}</div>
  {% endcomp %}
  {% endcomp %}
{% endcomp %}
